version: '3'
services:

  api:
    build: .
    ports:
      - 9000:8000
    volumes:
      - .:/home/node/brickdev
    depends_on:
      - mongo

  mongo:
    image: mongo:3.0-wheezy
    ports:
      - 27017
    volumes:
       - ./database:/data/db